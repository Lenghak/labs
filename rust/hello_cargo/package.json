{
  "name": "@rust/hello-cargo",
  "version": "0.1.0",
  "description": "Rust Hello World project",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build",
    "test": "cargo test",
    "check": "cargo check"
  }
}
